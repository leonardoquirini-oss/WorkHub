// Runtime configuration placeholder used by the Vite dev server and `vite preview`.
// In the Docker image this file is regenerated at container start by docker-entrypoint.sh.
// Any key left empty falls back to the build-time VITE_* value.
window.__WORKHUB_CONFIG__ = {};
